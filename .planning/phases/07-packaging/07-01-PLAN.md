---
phase: 07-packaging
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Gluey.Contract.Binary/Gluey.Contract.Binary.csproj
  - src/Gluey.Contract.Binary/README.md
  - .github/workflows/main.yml
autonomous: true
requirements: [PACK-01, PACK-02, PACK-03, PACK-05, PACK-07]

must_haves:
  truths:
    - "dotnet pack produces a .nupkg containing README.md and icon.png"
    - "CI pipeline has create-tag and publish jobs for contract-binary"
    - "README documents installation, contract loading, parsing, and value access"
    - "InternalsVisibleTo allows test project to access internal types"
  artifacts:
    - path: "src/Gluey.Contract.Binary/Gluey.Contract.Binary.csproj"
      provides: "NuGet metadata with PackageReadmeFile and PackageIcon"
      contains: "PackageReadmeFile"
    - path: "src/Gluey.Contract.Binary/README.md"
      provides: "Package documentation with usage examples"
      min_lines: 40
    - path: ".github/workflows/main.yml"
      provides: "CI pipeline with Binary package jobs"
      contains: "create-tag-contract-binary"
  key_links:
    - from: ".github/workflows/main.yml"
      to: "src/Gluey.Contract.Binary/Gluey.Contract.Binary.csproj"
      via: "csproj-path in create-tag and publish jobs"
      pattern: "csproj-path: src/Gluey.Contract.Binary/Gluey.Contract.Binary.csproj"
    - from: "src/Gluey.Contract.Binary/Gluey.Contract.Binary.csproj"
      to: "src/Gluey.Contract.Binary/README.md"
      via: "PackageReadmeFile metadata"
      pattern: "PackageReadmeFile.*README.md"
---

<objective>
Add NuGet package metadata, create README documentation, and configure CI pipeline for Gluey.Contract.Binary.

Purpose: Complete the packaging infrastructure so the Binary package can be built, packed, and published through CI exactly like the existing Json package.
Output: Updated csproj with PackageReadmeFile/PackageIcon, new README.md, CI pipeline with create-tag and publish jobs.
</objective>

<execution_context>
@C:/Users/kmmal/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/kmmal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Reference files the executor must read and mirror -->

From src/Gluey.Contract.Json/Gluey.Contract.Json.csproj (NuGet metadata pattern):
```xml
<PackageReadmeFile>README.md</PackageReadmeFile>
<PackageIcon>icon.png</PackageIcon>
...
<ItemGroup>
  <None Include="$(MSBuildThisFileDirectory)README.md" Pack="true" PackagePath="\" />
  <None Include="$(MSBuildThisFileDirectory)..\..\assets\icon.png" Pack="true" PackagePath="\" />
</ItemGroup>
```

From .github/workflows/main.yml (CI job pattern for Json):
```yaml
create-tag-contract-json:
  if: github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'workflow_dispatch')
  needs: [build-and-test]
  uses: rebels-software/github-actions/.github/workflows/create-version-tag.yaml@v1.1.0
  with:
    csproj-path: src/Gluey.Contract.Json/Gluey.Contract.Json.csproj
    tag-prefix: contract-json

publish-contract-json:
  needs: [build-and-test, create-tag-contract-json]
  if: always() && needs.build-and-test.result == 'success' && (startsWith(github.ref, 'refs/tags/contract-json/v') || needs.create-tag-contract-json.result == 'success')
  uses: rebels-software/github-actions/.github/workflows/publish-nuget.yaml@v1.1.0
  with:
    csproj-path: src/Gluey.Contract.Json/Gluey.Contract.Json.csproj
    dotnet-version: |
      9.0.x
      10.0.x
  secrets:
    NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add NuGet metadata and create README</name>
  <read_first>
    - src/Gluey.Contract.Binary/Gluey.Contract.Binary.csproj
    - src/Gluey.Contract.Json/Gluey.Contract.Json.csproj
    - src/Gluey.Contract.Json/README.md
    - docs/adr/16-binary-format-contract.md
  </read_first>
  <files>src/Gluey.Contract.Binary/Gluey.Contract.Binary.csproj, src/Gluey.Contract.Binary/README.md</files>
  <action>
    **Step 1: Update csproj** -- Add two properties to the existing PropertyGroup and one new ItemGroup.

    Add to the PropertyGroup (after the existing PackageTags line):
    ```xml
    <PackageReadmeFile>README.md</PackageReadmeFile>
    <PackageIcon>icon.png</PackageIcon>
    ```

    Add a new ItemGroup (after the ProjectReference ItemGroup):
    ```xml
    <ItemGroup>
      <None Include="$(MSBuildThisFileDirectory)README.md" Pack="true" PackagePath="\" />
      <None Include="$(MSBuildThisFileDirectory)..\..\assets\icon.png" Pack="true" PackagePath="\" />
    </ItemGroup>
    ```

    Do NOT change Version (already 1.0.0 per D-06). Do NOT change InternalsVisibleTo (already configured).

    **Step 2: Create README.md** -- Mirror the Json README structure per D-04. Use these exact sections:

    1. Title: `# Gluey.Contract.Binary` with NuGet badge links (same pattern as Json but for `Gluey.Contract.Binary` package)
    2. One-line description: Zero-allocation, schema-driven binary protocol parser for .NET. Parses custom binary payloads in a single pass against a JSON contract definition.
    3. "Part of Gluey.Contract" link (same as Json)
    4. Installation: `dotnet add package Gluey.Contract.Binary`
    5. Quick Start per D-05 with representative mixed types. Use this contract JSON:
    ```json
    {
      "kind": "binary",
      "id": "sensor/telemetry",
      "name": "telemetry",
      "version": "1.0.0",
      "endianness": "little",
      "fields": {
        "recordedAgo": {
          "type": "uint16",
          "size": 2,
          "validation": { "min": 0, "max": 3600 }
        },
        "deviceId": {
          "dependsOn": "recordedAgo",
          "type": "string",
          "size": 8,
          "encoding": "ascii"
        },
        "status": {
          "dependsOn": "deviceId",
          "type": "enum",
          "size": 1,
          "values": { "0": "idle", "1": "active", "2": "error" }
        }
      }
    }
    ```
    And this C# parse example:
    ```csharp
    var schema = BinaryContractSchema.Load(contractJson);
    var result = schema!.Parse(payloadBytes);

    if (result is { } parsed && parsed.IsValid)
    {
        parsed["recordedAgo"].GetUInt16();   // e.g. 120
        parsed["deviceId"].GetString();       // e.g. "SENS0042"
        parsed["status"].GetString();         // e.g. "active"
    }
    ```
    6. Features section listing: Zero allocation (ParsedProperty is a readonly struct), Single pass (validation and indexing in one traversal), Contract-driven (JSON contract defines binary layout), Endianness support (big-endian and little-endian)
    7. Supported field types table:
    | Type | Description |
    | uint8, uint16, uint32 | Unsigned integers |
    | int8, int16, int32 | Signed integers |
    | float32, float64 | Floating point |
    | boolean | Boolean (0 = false, non-zero = true) |
    | string | Fixed-length ASCII or UTF-8 |
    | enum | Mapped value sets |
    | bits | Bit field containers with sub-fields |
    | struct (in arrays) | Composite types |
    | padding | Skip bytes |
    8. License link: `[Apache 2.0](https://github.com/rebels-software/gluey-contract/blob/main/LICENSE)`
  </action>
  <verify>
    <automated>dotnet pack src/Gluey.Contract.Binary -c Release --no-restore -o .artifacts 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "PackageReadmeFile" src/Gluey.Contract.Binary/Gluey.Contract.Binary.csproj
    - grep -q "PackageIcon" src/Gluey.Contract.Binary/Gluey.Contract.Binary.csproj
    - grep -q 'PackagePath="\\"' src/Gluey.Contract.Binary/Gluey.Contract.Binary.csproj
    - test -f src/Gluey.Contract.Binary/README.md
    - grep -q "Gluey.Contract.Binary" src/Gluey.Contract.Binary/README.md
    - grep -q "BinaryContractSchema.Load" src/Gluey.Contract.Binary/README.md
    - grep -q "dotnet add package" src/Gluey.Contract.Binary/README.md
    - grep -q "GetUInt16" src/Gluey.Contract.Binary/README.md
  </acceptance_criteria>
  <done>
    - Csproj has PackageReadmeFile and PackageIcon properties
    - Csproj has ItemGroup packing README.md and icon.png into the NuGet package
    - README.md exists with Installation, Quick Start (contract + C# parse), Features, Supported field types, License
    - dotnet pack succeeds and produces .nupkg
  </done>
</task>

<task type="auto">
  <name>Task 2: Add CI pipeline jobs for Binary package</name>
  <read_first>
    - .github/workflows/main.yml
  </read_first>
  <files>.github/workflows/main.yml</files>
  <action>
    Make three additions to `.github/workflows/main.yml`:

    **Addition 1: Tag trigger** -- Add `'contract-binary/v*'` to the `on.push.tags` array (after the existing `'contract-aspnetcore/v*'` entry):
    ```yaml
    tags:
      - 'contract/v*'
      - 'contract-json/v*'
      - 'contract-aspnetcore/v*'
      - 'contract-binary/v*'
    ```

    **Addition 2: Create-tag job** -- Add after the `create-tag-contract-aspnetcore` job (before the publish jobs):
    ```yaml
    create-tag-contract-binary:
      if: github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'workflow_dispatch')
      needs: [build-and-test]
      uses: rebels-software/github-actions/.github/workflows/create-version-tag.yaml@v1.1.0
      with:
        csproj-path: src/Gluey.Contract.Binary/Gluey.Contract.Binary.csproj
        tag-prefix: contract-binary
    ```

    **Addition 3: Publish job** -- Add after the `publish-contract-aspnetcore` job (at the end of the file):
    ```yaml
    publish-contract-binary:
      needs: [build-and-test, create-tag-contract-binary]
      if: always() && needs.build-and-test.result == 'success' && (startsWith(github.ref, 'refs/tags/contract-binary/v') || needs.create-tag-contract-binary.result == 'success')
      uses: rebels-software/github-actions/.github/workflows/publish-nuget.yaml@v1.1.0
      with:
        csproj-path: src/Gluey.Contract.Binary/Gluey.Contract.Binary.csproj
        dotnet-version: |
          9.0.x
          10.0.x
      secrets:
        NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
    ```

    Do NOT modify existing jobs. Do NOT add a separate test job (per D-03 -- Binary tests run in existing build-and-test job). Use tag-prefix `contract-binary` per D-02.
  </action>
  <verify>
    <automated>grep -c "contract-binary" .github/workflows/main.yml</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "contract-binary/v\*" .github/workflows/main.yml
    - grep -q "create-tag-contract-binary" .github/workflows/main.yml
    - grep -q "publish-contract-binary" .github/workflows/main.yml
    - grep -q "tag-prefix: contract-binary" .github/workflows/main.yml
    - grep -q "csproj-path: src/Gluey.Contract.Binary/Gluey.Contract.Binary.csproj" .github/workflows/main.yml
  </acceptance_criteria>
  <done>
    - on.push.tags includes 'contract-binary/v*'
    - create-tag-contract-binary job exists with correct csproj-path and tag-prefix
    - publish-contract-binary job exists with correct if-condition, csproj-path, dotnet-version, and NUGET_API_KEY secret
    - No separate test job added (Binary tests run in build-and-test per D-03)
  </done>
</task>

</tasks>

<verification>
1. `dotnet build src/Gluey.Contract.Binary -c Release --no-restore -q` -- builds successfully
2. `dotnet pack src/Gluey.Contract.Binary -c Release --no-restore -o .artifacts` -- pack succeeds, produces .nupkg
3. `dotnet test tests/Gluey.Contract.Binary.Tests --no-build -q` -- all 328 tests pass (regression check)
4. `grep "contract-binary" .github/workflows/main.yml` -- shows tag trigger, create-tag job, and publish job
5. `test -f src/Gluey.Contract.Binary/README.md` -- README exists
</verification>

<success_criteria>
- Binary package builds and packs with README and icon included in .nupkg
- CI pipeline has create-tag-contract-binary and publish-contract-binary jobs matching Json pattern exactly
- README documents installation, contract definition, parsing, value access, features, and supported field types
- All existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/07-packaging/07-01-SUMMARY.md`
</output>
