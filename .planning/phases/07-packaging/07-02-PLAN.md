---
phase: 07-packaging
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [PACK-04, PACK-05]

must_haves:
  truths:
    - "All 328+ tests pass across all field types"
    - "Test project can access internal types via InternalsVisibleTo"
    - "Code coverage report is generated showing coverage across all feature areas"
  artifacts:
    - path: "tests/Gluey.Contract.Binary.Tests/Gluey.Contract.Binary.Tests.csproj"
      provides: "Test project configuration with coverlet"
      contains: "coverlet.collector"
  key_links:
    - from: "tests/Gluey.Contract.Binary.Tests/Gluey.Contract.Binary.Tests.csproj"
      to: "src/Gluey.Contract.Binary/Gluey.Contract.Binary.csproj"
      via: "ProjectReference + InternalsVisibleTo"
      pattern: "ProjectReference.*Gluey.Contract.Binary.csproj"
---

<objective>
Verify code coverage meets project standards and document test coverage across all field types.

Purpose: Confirm PACK-04 (high code coverage) and PACK-05 (InternalsVisibleTo) are satisfied by the existing test suite. No new test files needed -- this is a verification-only plan.
Output: Coverage report confirming test coverage across contract loading, scalar parsing, leaf types, composites, and validation.
</objective>

<execution_context>
@C:/Users/kmmal/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/kmmal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify test coverage and InternalsVisibleTo</name>
  <read_first>
    - tests/Gluey.Contract.Binary.Tests/Gluey.Contract.Binary.Tests.csproj
    - src/Gluey.Contract.Binary/Gluey.Contract.Binary.csproj
  </read_first>
  <files></files>
  <action>
    This is a verification-only task. No files are modified.

    **Step 1: Verify InternalsVisibleTo** -- Confirm `src/Gluey.Contract.Binary/Gluey.Contract.Binary.csproj` contains:
    ```xml
    <InternalsVisibleTo Include="Gluey.Contract.Binary.Tests" />
    ```
    This satisfies PACK-05.

    **Step 2: Run full test suite** -- Execute `dotnet test tests/Gluey.Contract.Binary.Tests --verbosity normal` and confirm all tests pass. Report the test count.

    **Step 3: Generate coverage report** -- Execute:
    ```bash
    dotnet test tests/Gluey.Contract.Binary.Tests --collect:"XPlat Code Coverage" --results-directory .artifacts/coverage
    ```
    Then find the coverage XML and report the line coverage percentage. The coverage report confirms test breadth across all feature areas (contract loading, chain resolution, endianness, scalars, strings, enums, bits, composites, validation).

    **Step 4: Summarize coverage** -- Report which test files cover which feature areas:
    - Contract loading and validation tests
    - Chain resolution tests
    - Endianness tests
    - Scalar parsing tests
    - Leaf type tests (string, enum, bits, padding)
    - Composite type tests (arrays, structs)
    - Validation tests (min/max, pattern, length)

    If any major feature area lacks test coverage, create a follow-up note. Based on research (328 tests, 7 test files), coverage should be comprehensive.
  </action>
  <verify>
    <automated>dotnet test tests/Gluey.Contract.Binary.Tests --verbosity minimal 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - dotnet test exits with code 0 (all tests pass)
    - grep -q "InternalsVisibleTo.*Gluey.Contract.Binary.Tests" src/Gluey.Contract.Binary/Gluey.Contract.Binary.csproj
    - Test count is 328 or higher
  </acceptance_criteria>
  <done>
    - All existing tests pass (328+ tests)
    - InternalsVisibleTo confirmed for Gluey.Contract.Binary.Tests
    - Coverage report generated showing coverage across all feature areas
    - PACK-04 and PACK-05 verified as satisfied
  </done>
</task>

</tasks>

<verification>
1. `dotnet test tests/Gluey.Contract.Binary.Tests` -- all tests pass
2. `grep "InternalsVisibleTo" src/Gluey.Contract.Binary/Gluey.Contract.Binary.csproj` -- shows test project name
3. Coverage report exists in .artifacts/coverage/
</verification>

<success_criteria>
- All 328+ tests pass across all field types
- InternalsVisibleTo confirmed for test project
- Coverage report generated and coverage is comprehensive across all feature areas
</success_criteria>

<output>
After completion, create `.planning/phases/07-packaging/07-02-SUMMARY.md`
</output>
